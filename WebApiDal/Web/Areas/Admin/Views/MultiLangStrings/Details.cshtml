@model Domain.MultiLangString

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>MultiLangString</h4>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MultiLangStringId)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.MultiLangStringId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Value)
        </dt>
        <dd>
            @Html.Raw(Model.Value)
        </dd>
    </dl>
    <hr/>
    <dl class="dl-horizontal">
        @foreach (var translation in Model.Translations)
        {
            <dt>
                @Html.DisplayNameFor(t => translation.TranslationId)
            </dt>
            <dd>
                @Html.DisplayFor(t => translation.TranslationId)
            </dd>

            <dt>
                @Html.DisplayNameFor(t => translation.Culture)
            </dt>
            <dd>
                @Html.DisplayFor(t => translation.Culture)
            </dd>

            <dt>
                @Html.DisplayNameFor(t => translation.Value)
            </dt>
            <dd>
                @Html.Raw(translation.Value)
            </dd>
        }
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new {id = Model.MultiLangStringId}) |
    @Html.ActionLink("Back to List", "Index")
</p>